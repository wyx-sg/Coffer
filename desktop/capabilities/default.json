{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capability for the Coffer desktop window. No native APIs are exposed yet; capabilities will be added per feature spec.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
