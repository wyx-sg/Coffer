# PR 4 brief — openclaw as Coffer's 6th managed agent

Repo: /Users/xing/Coffer (work in worktree /Users/xing/Coffer-wt/openclaw-agent, branch feat/openclaw-agent off origin/main AFTER PR #279 merges — it supplies plugin_drop.py + the raw dialect).

## Program context
User-authorized all-six-agents program. Slice recipe (validated on opencode #246, hermes #247, cursor #258, PLUGIN_DROP #279): spec-first, one PR, real e2e probes for every upstream claim, independent review, merge on green. ADR-042 discipline: every capability claim cites a doc or a probe.

## Probe-verified upstream facts (openclaw 2026.6.11, installed + onboarded)
- Chat: `openclaw agent --agent main --session-key <key> -m <text> --json --local` → stdout is ONE clean JSON blob (`payloads[].text` = reply; also `finalAssistantVisibleText`, `stopReason`, `completion.finishReason`, `executionTrace.winnerProvider/winnerModel`); logs go to stderr. NON-STREAMING (Coffer's first). `--local` = fully embedded, no gateway daemon needed, plugin registry preloaded per run. Same session-key → same session (history at `~/.openclaw/agents/<id>/sessions/*.jsonl`). NO cwd flag — runs in the agent's workspace (`agents.defaults.workspace`, default `~/.openclaw/workspace`); the adapter must IGNORE conversation cwd (document this: openclaw is a personal-assistant gateway, not a repo-scoped CLI).
- MCP: `mcp.servers.<name>` in `~/.openclaw/openclaw.json` (plain JSON): `{command, args, env, cwd, enabled?}`. Live config has NO mcp block yet. CRITICAL: openclaw REJECTS interpreter-startup env keys (NODE_OPTIONS, PYTHONPATH, …) inside a server's env — config error at startup. Tools namespaced `<server>__<tool>`; exposed under tools.profile "coding" (live value).
- Plugins: local plugin = a PACKAGE DIR in `~/.openclaw/extensions/<id>/` with (a) package.json carrying `"openclaw": {"extensions": ["./index.js"]}`, (b) `openclaw.plugin.json` manifest `{"id": "<id>", "configSchema": {"type":"object","additionalProperties":false}}`, (c) entry exporting `definePluginEntry({id, name, register(api){...}})` from "openclaw/plugin-sdk/plugin-entry". MUST also be enabled: `plugins.entries.<id>.enabled: true` (fail-closed for non-bundled). Prompt injection hook: `api.on("before_prompt_build", handler)` — handler RETURNS `{appendSystemContext: "<text>"}` (or prependContext etc.). Plugins run in-process in the gateway/embedded node at full privilege → CAN spawn coffer-hook. NOTE: gateway restarts are needed after plugin changes for the long-running gateway, but `--local` embedded runs preload fresh each run → Coffer-driven turns pick the plugin up immediately; document the gateway-restart caveat for channel-driven openclaw use.
- Prompt-injection gating keys exist: `plugins.entries.<id>.hooks.allowPromptInjection` (default allows prompt hooks); conversation-access gate `allowConversationAccess` is only for raw conversation hooks (not needed for before_prompt_build).
- Provider projection: `models.providers.<id> = {api, baseUrl, apiKey, models: [{id, name}]}` — models[].name is REQUIRED (probe: "models.0.name: Invalid input" without it); model refs `provider/model` (first slash); active model via `agents.defaults.model.primary` (PRESERVE the user's fallbacks list). `apiKey` supports `${UPPERCASE_VAR}`. PROBE RESULT (2026-07-10, overrides the docs' "throws at config load" claim): a missing env var is a startup WARNING only — "Missing env var … feature using this value will be unavailable", exit 0, other providers unaffected. → Provider projection IS viable, codex-style: persist `apiKey: "${COFFER_PROVIDER_KEY}"`, inject the key into the subprocess env per Coffer-driven turn (resolve_key seam); a user-run openclaw without the var degrades gracefully. Cite the probe in the matrix cell, not the docs.
- Native memory disable: set `plugins.slots.memory: "none"` (preferred single key; restore = remove the key). Live config has no plugins.slots block. Transform in native_memory_disable.py (JSON).
- Skills: `~/.openclaw/skills/<name>/SKILL.md`; SYMLINKED skill folders ARE discovered (realpath must stay in the resolved dir — Coffer's symlink of the master folder satisfies this). skill_subpath="skills", FOLDER mode, default machinery works.
- Instruction files (workspace, live-verified present): AGENTS.md, SOUL.md, IDENTITY.md, USER.md, TOOLS.md, MEMORY.md under `~/.openclaw/workspace/`. Allowlist should expose config (`openclaw.json`, JSON) + the workspace instruction files (MARKDOWN) — paths cfg/"workspace"/<file> assuming default workspace (document the assumption; agents.defaults.workspace is configurable).
- Detection: default marker `~/.openclaw` (env overrides exist — OPENCLAW_STATE_DIR etc. — treat like other agents' default dirs; do not implement env resolution).

## Deliverables (pattern: copy the opencode slice shape everywhere)
1. `AgentType.OPENCLAW = "openclaw"` (types.py) — NO DB migration needed (re-adding a value).
2. `allowlists.py`: `_openclaw_files` — config + workspace instruction files (6 markdown entries or a curated subset: config, agents_md, soul, identity, user, tools, memory_md; pick stable keys; kind FILE).
3. Descriptor record: display_name "OpenClaw", config_subpath ".openclaw", mcp=McpInjectionSpec(config_key="config", container "mcp" → nested "servers": CHECK mcp_install transforms for nested-container support; opencode used TYPED_LOCAL_OBJECT in container "mcp"; openclaw needs `mcp.servers.coffer = {command, args}` — if the transform can't nest, extend it (a `container_path: tuple[str,...]` or reuse existing style if COMMAND_MAP under a dotted path exists). Coffer's entry: command=shim abs path, args=[] (match other agents' shim invocation — read mcp_install.py for the exact shape used for claude).
4. Context injection: PLUGIN_DROP with a SECOND plugin flavor. plugin_drop.py currently renders opencode's flat .js. Add an openclaw renderer producing THREE files (package.json / openclaw.plugin.json / index.js with definePluginEntry + before_prompt_build returning {appendSystemContext}) — plugin id "coffer-session-context". index.js spawns coffer-hook (same argv as opencode: --agent <name> --dialect raw --event sessionStart), setEncoding("utf8"), 10s timeout + settle guard, cache per sessionId if available in hook args else "global", cache-success-only. Install ALSO sets `plugins.entries.coffer-session-context.enabled: true` in openclaw.json (JSON transform; uninstall removes the entry AND the extension dir). ContextInjectionSpec needs a discriminator for the plugin flavor — mirror HookFlavor: add `PluginFlavor` enum (OPENCODE flat file / OPENCLAW package dir) on the spec or key off agent type in hook_service dispatch (prefer a spec field; keep hook_service generic).
5. Native memory disable: `_NATIVE_MEMORY_DISABLE_TARGET[OPENCLAW] = ("config", JSON)` + transform: set `plugins.slots.memory = "none"`; restore removes slots.memory (and empty slots object). Follow native_memory_disable.py's existing pure-transform pattern + tests.
6. Chat adapter: openclaw_run.py (subprocess exec `openclaw agent --agent main --session-key <key> -m <text> --json --local`, model override via `--model` when conversation has one), openclaw_mapping.py (parse the JSON blob → TurnStarted, one TextDelta(payloads[].text joined or finalAssistantVisibleText), TurnDone(stop_reason from completion.finishReason); model_id from executionTrace), openclaw_agent.py (AgentAdapter), openclaw_provider.py (AgentProvider, agent_key "openclaw", availability = `openclaw` on PATH, session key = coffer conversation id, e.g. `coffer-<conversation_id>`; NO cwd handling — ignore config.cwd with a comment; resolve_key: YES — projection is viable per the probe; register with resolve_key=_key_resolver(AgentType.OPENCLAW) like codex/opencode/hermes, injecting COFFER_PROVIDER_KEY into the subprocess env). Register in chat_provider_wiring.py. Pattern files: cursor_run/cursor_mapping/cursor_agent/cursor_provider + opencode_* (read them first).
7. Frontend: AgentType union (lib/api/agents.ts, lib/api/providers.ts), AgentManualAddForm TYPES, i18n labels agentOpenclaw (en+zh), connectionPresets: add openclaw to SELECTABLE_AGENTS + defaultCompatibleAgents for openai + anthropic wires? — openclaw supports openai-completions/openai-responses AND anthropic-messages apis, so add to BOTH wire defaults; domain/provider/config.py _KNOWN_AGENTS + backend _DEFAULT_COMPATIBLE + projection.py _AGENT_TARGETS (new openclaw JSON projection transform in domain/provider/projection.py — read its existing per-agent writers first; PRESERVE user fallbacks; models entry needs {id, name}).
8. Spec 004 (en+zh): FR-003 type list, FR-013 allowlist, capability matrix row (cite every cell: probes above), scenarios (MCP inject? reuse existing generic scenarios — check what opencode's slice added; at minimum: chat adapter mention in spec 008 if that spec lists adapters, plugin-drop install scenario for openclaw if the shape differs enough to warrant one), amendment header note (openclaw joins as 6th type, reversing ADR-040's exclusion per ADR-042 correction + research doc). Roadmap 004 row update. New ADR-043 (openclaw is a managed leaf agent — records the reversal, the capability matrix w/ citations, the non-streaming chat adapter, the no-cwd semantics, the projection decision, the gateway-restart caveat) en+zh.
9. Tests: descriptor/allowlist unit; plugin renderer unit (node --check the rendered index.js); mcp install integration (nested container); native-memory transform unit+integration; hook service PLUGIN_DROP openclaw integration (three files + entries.enabled toggle + uninstall inverse); adapter/mapping unit with captured JSON fixture (use the real blob shape from the probe: payloads/finalAssistantVisibleText/stopReason/executionTrace/completion); provider integration patterned on cursor_provider tests. Acceptance markers byte-exact with new scenario headings.
10. E2E probes to RUN during implementation (cheap, use scratch OPENCLAW_CONFIG_PATH/STATE_DIR where possible; deepseek-flash for one live turn ≈ pennies):
   a. Projection load-failure probe (above, scratch config).
   b. MCP injection: write mcp.servers.coffer into a SCRATCH config pointing at a fake stdio server; `openclaw mcp status` (read-only) with OPENCLAW_CONFIG_PATH → entry listed.
   c. Plugin drop: render the 3 files into scratch OPENCLAW_STATE_DIR extensions dir + entries.enabled in scratch config; run ONE live turn with a stub coffer-hook printing a marker; ask model to repeat the token (open-phrase, NOT yes/no — free/cheap models flake on yes/no).
   d. Chat adapter smoke vs the REAL openclaw (user's config): one turn `--session-key coffer-adapter-probe` asking to reply PONG; assert parse. (Already proven manually; re-run through the adapter code.)
11. Gates: ruff check + `ruff format --check` + `scripts/check_file_sizes.py` (NO pipe-masking of exit codes; run AFTER any merge of main) + PYTHONPATH-shadow mypy + full pytest + audit_acceptance + frontend (symlink node_modules: `ln -s /Users/xing/Coffer/frontend/node_modules <wt>/frontend/node_modules`, tsc/eslint/vitest; rm the symlink before worktree removal). docs-site risk: no spec line may START with `<`.

## Cautions (from this program's history)
- PostToolUse ruff-autofix strips imports added before their usages exist — add usage same edit or re-add imports last.
- The user merges to main in parallel: expect BEHIND/DIRTY; merge origin/main into the branch (force-push is blocked by the harness), re-run check_file_sizes after EVERY merge.
- One commit per PR initially; review fixes land as additional commits (squash-merge collapses).
- Commit subject ≤72 chars; Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>.
- Do NOT touch the main checkout (/Users/xing/Coffer) working tree — the user works there.
- Never write to the user's real ~/.openclaw/openclaw.json during tests/probes except the final supervised adapter smoke (read-only) — use OPENCLAW_CONFIG_PATH/OPENCLAW_STATE_DIR scratch overrides.
