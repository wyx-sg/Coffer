"""The workflow engine: templates, runs, nodes, approvals and the gate.

Every seam to another kind is a Protocol in :mod:`ports`; nothing here imports
the turn platform, the gateway, the knowledge layer or a channel.
"""
