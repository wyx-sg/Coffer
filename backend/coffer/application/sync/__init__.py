"""Sync application layer (spec vault-export-import): the vault export/import service and the
exporter/importer it orchestrates. Talks to the filesystem and credentials only
through the ports in ``ports.py``."""
