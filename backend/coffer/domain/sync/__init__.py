"""Vault convergence domain (spec vault-sync, ADR: vault-sync).

Pure value objects and contracts: the manifest of the tree the vault converges
through, what one serialization reports, deterministic resource serialization,
path portability, and the error family. No filesystem, no database — those live
in infrastructure.
"""
