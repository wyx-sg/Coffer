"""Domain — pure entities, value objects, domain services.

Must not import any other coffer layer or external SDK (FastAPI, SQLAlchemy,
httpx, keyring, etc.). Python stdlib + Pydantic only. See agents/stack.md.
"""
