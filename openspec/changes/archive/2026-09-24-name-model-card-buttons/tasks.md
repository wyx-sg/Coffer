## 1. Code

- [x] 1.1 `AgentModelCatalogueService.model_labels`: name, else id; "1M" for a `[1m]` id
- [x] 1.2 `model_card` takes `labels`; the `/model` command and a page turn pass them

## 2. Tests

- [x] 2.1 Catalogue labels; card shows names and carries ids

## 3. Specs

- [x] 3.1 channels delta
