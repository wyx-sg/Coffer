## 1. Code

- [x] 1.1 `button_rows` packs buttons by label width, falling back to even rows over three
- [x] 1.2 `interactive_card` uses it

## 2. Tests

- [x] 2.1 Long label on its own row (the live `/agent` and `/model` cards), wide characters, even fallback

## 3. Specs

- [x] 3.1 channels/seatalk delta
