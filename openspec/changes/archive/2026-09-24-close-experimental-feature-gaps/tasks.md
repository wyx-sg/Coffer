## 1. Code

- [x] 1.1 The registry maps kinds to features, and the resource routes refuse or leave out a switched-off feature's resources
- [x] 1.2 Routing takes each router's gate from the registry's route prefixes
- [x] 1.3 Curation treats the vault as single-machine while `vault_sync` is off
- [x] 1.4 The handshake instructions and the `coffer-guide` skill name only the tools the tool list carries, and the guide re-renders on a `knowledge` or `memory` switch
- [x] 1.5 Feature switches are serialized; the memory hook reconciles to the current state
- [x] 1.6 The CLI names an outdated daemon; the web machine picker does not hang on an offline daemon

## 2. Specs and docs

- [x] 2.1 Deltas for experimental-features and knowledge
- [x] 2.2 The resource-framework contract documents `FEATURE_DISABLED`
- [x] 2.3 Every new scenario's test carries its acceptance marker

## 3. Close

- [x] 3.1 Run the gates
- [x] 3.2 Archive the change
